{{- if and .Values.db.enabled .Values.db.enableLegacy -}}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "supabase.db.fullname" . }}-migrations
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.db.legacy.config | nindent 2 }}
{{- end }}