{{- if .Values.db.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supabase.initdb.secret" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
type: Opaque
stringData:
  schema-setup.sql: |
    -- Create _analytics schema (from 99-logs.sql)
    CREATE SCHEMA IF NOT EXISTS _analytics;
    ALTER SCHEMA _analytics OWNER TO {{ .Values.secret.db.username | default "postgres" }};

    -- Create _realtime schema (from 99-realtime.sql)
    CREATE SCHEMA IF NOT EXISTS _realtime;
    ALTER SCHEMA _realtime OWNER TO {{ .Values.secret.db.username | default "postgres" }};

    -- Create supabase_functions schema (from 98-webhooks.sql - basic setup)
    CREATE SCHEMA IF NOT EXISTS supabase_functions AUTHORIZATION supabase_admin;
    GRANT USAGE ON SCHEMA supabase_functions TO postgres, anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA supabase_functions GRANT ALL ON TABLES TO postgres, anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA supabase_functions GRANT ALL ON FUNCTIONS TO postgres, anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA supabase_functions GRANT ALL ON SEQUENCES TO postgres, anon, authenticated, service_role;
{{- end }}
